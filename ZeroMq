Req行为：如果使用Req发送数据Data，那么Req将为数据封装成两帧：空帧+Data帧; Req收到的数据也必须为空帧+Data，将空帧去掉，把Data帧提交给用户。

Rep行为：如果收到“地址帧”+空帧+“Data，把“Data”帧提交给用户，下次发送组合成“地址帧”+空帧+Data。如果收到空帧+“Data，把“Data”帧提交给用户，下次发送组合成：空帧+Data

Router行为：收到数据增加“地址”，将“地址帧”+空帧+Data提交给用户; 发送时数据格式必须为“地址帧”+空帧+Data，将空帧+Data发送到对端

Dealer行为：用户发什么，接受端就收什么; 收到什么，就给用户提交什么。Dealer连接多个接受端时，按照负载均衡的策略选择一个接受端发送数据。
